import React from "react";
import { Container } from "@/components";

export const MenuRoute: React.FC = () => {
  return (
    <section>
      <Container>
        <h1>Menu</h1>
      </Container>
    </section>
  );
};
