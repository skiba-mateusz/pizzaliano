import React from "react";
import { Container } from "@/components";

export const OrderRoute: React.FC = () => {
  return (
    <section>
      <Container>
        <h1>Order</h1>
      </Container>
    </section>
  );
};
